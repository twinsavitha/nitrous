
<%= form_for([@publish, @publish.comments.build]) do |f| %>
  <div class="field" id="bio">
  <p>
    <%= f.label :commenter %><br>
    <%= f.text_field :commenter %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
  </div>
<% end %>